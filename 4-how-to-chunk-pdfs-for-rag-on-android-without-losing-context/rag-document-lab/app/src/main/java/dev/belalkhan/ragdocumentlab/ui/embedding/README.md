# Educational embedding viewer

This directory contains the screen that displays the exact chunk text and embedding vectors saved in Room.

It exists only to demonstrate what an embedding looks like and how it is stored. A normal application does not need to expose raw embedding values in its user interface.

The screen reads existing vectors from local storage. It does not run EmbeddingGemma again.
